get:
  summary: Pings the server to verfy connection
  responses:
    "418":
      description: I'm a teapot
      content:
        application/json:
          schema:
            type: object
            required:
              - message
            properties:
              message:
                type: string
